import React from 'react';
import {useUserStatus} from "../../customHooks";

import './useCallback&useMemo.scss';

type TUserInfo = {
    name: string;
}
export function ComponentUseCallback(props: React.PropsWithChildren<TUserInfo>) {

    return (
        <div>

        </div>
    );
}
